# 从零开始制作个人网页 —— 原理篇

## 一、网页的本质是什么？

在动手写代码之前，先理解一个根本问题：**当你在浏览器里打开一个网页时，到底发生了什么？**

```
你输入网址 → 浏览器向服务器发送 HTTP 请求 → 服务器返回 HTML 文件 → 浏览器解析并渲染成你看到的页面
```

用更生活化的比喻：

> 网页就像一封**带有排版说明的信**。你（浏览器）收到这封信，按照信里的排版说明，把它"画"在屏幕上。这封信就是 **HTML 文件**，排版说明是 **CSS**，信里可能还附带一些**可以交互的小程序**（JavaScript）。

---

## 二、需要什么"解释器"吗？

这是很多人困惑的地方。当你用 Python 写程序，你需要安装 Python 解释器。当你用 C++ 写程序，你需要编译器。

**网页的核心语言不需要你额外安装任何解释器——浏览器就是解释器。**

| 语言 | 作用 | 解释器在哪？ |
|------|------|------------|
| **HTML** | 定义页面结构和内容 | 每个浏览器（Chrome、Edge、Firefox、Safari）都内置了解析引擎 |
| **CSS** | 定义页面样式（颜色、布局、字体） | 同上，浏览器内置 |
| **JavaScript** | 定义页面交互逻辑（点击、动画、数据加载） | 同上，浏览器内置 JS 引擎（V8、SpiderMonkey 等） |

这意味着：**你只需要一个文本编辑器（记事本都行）和一个浏览器，就能制作网页。** 不需要安装 Python，不需要安装 Node.js，不需要任何运行时。

---

## 三、三种语言的分工

### 3.1 HTML —— 骨架

HTML（HyperText Markup Language，超文本标记语言）不是"编程语言"，而是**标记语言**。它用标签（tag）来描述内容的结构。

```html
<!DOCTYPE html>
<html>
  <head>
    <title>我的个人网页</title>
  </head>
  <body>
    <h1>你好，我是张三</h1>
    <p>欢迎来到我的个人主页。</p>
    <img src="photo.jpg" alt="我的照片">
  </body>
</html>
```

每个 `<xxx>` 就是一个标签，浏览器读到 `<h1>` 就知道"这是一个一级标题"，读到 `<p>` 就知道"这是一个段落"。

### 3.2 CSS —— 皮肤

CSS（Cascading Style Sheets，层叠样式表）控制页面长什么样。

```css
body {
  background-color: #f0f0f0;  /* 背景灰色 */
  font-family: "Microsoft YaHei", sans-serif;  /* 字体 */
}

h1 {
  color: #333;        /* 标题深灰色 */
  font-size: 32px;    /* 字号 32 像素 */
  text-align: center; /* 居中 */
}
```

### 3.3 JavaScript —— 肌肉

JavaScript 是真正的**编程语言**，它让页面"活"起来。

**但首先，区分一个关键概念：**

很多新手以为"点击跳转"需要 JavaScript。实际上，**点击跳转是 HTML 原生能力**，`<a>` 标签就能做到，完全不需要 JS：

```html
<!-- 这就是点击跳转 —— 纯 HTML，不需要 JavaScript -->
<a href="/about.html">关于我</a>
<a href="https://github.com">我的 GitHub</a>
<a href="mailto:hello@example.com">发邮件</a>
```

那 JavaScript 的"交互"到底指什么？**指页面不跳转、不刷新，内容自己发生变化：**

```
┌──────────────────────────────────────────────────────────┐
│  HTML 能做的交互（无需 JS）     │  JavaScript 能做的交互    │
├────────────────────────────────┼──────────────────────────┤
│ 点击链接 → 跳转到另一个页面      │ 点击按钮 → 弹出一个对话框  │
│ 提交表单 → 跳转到结果页         │ 点击按钮 → 展开/收起内容   │
│ 点击锚点 → 滚动到页内位置       │ 输入文字 → 实时搜索结果     │
│                                │ 点击图片 → 放大查看        │
│                                │ 滚动页面 → 元素淡入动画     │
│                                │ 切换标签 → 切换显示内容     │
│                                │ 拖拽排序、地图交互……       │
└──────────────────────────────────────────────────────────┘
```

一句话区分：**HTML 交互 = "去另一个页面"。JS 交互 = "当前页面自己变"。**

下面是几个具体的 JavaScript 交互例子：

```javascript
// 例子1：点击按钮，切换暗色模式（页面不变，样式变了）
document.getElementById("theme-btn").addEventListener("click", function() {
  document.body.classList.toggle("dark-mode");
});

// 例子2：点击"展开更多"，显示隐藏的内容
document.getElementById("expand-btn").addEventListener("click", function() {
  document.getElementById("more-content").style.display = "block";
});

// 例子3：在搜索框里打字，实时筛选列表
document.getElementById("search").addEventListener("input", function(e) {
  let keyword = e.target.value;
  // 遍历列表项，隐藏不匹配的……
});
```

---

## 四、网页的运行机制（浏览器如何工作）

这是理解网页开发最关键的原理：

```
┌─────────────────────────────────────────────────┐
│                   浏览器                         │
│                                                 │
│  1. 下载 HTML 文件                               │
│       ↓                                         │
│  2. 解析 HTML，构建 DOM 树（Document Object Model）│
│       ↓                                         │
│  3. 遇到 <link> 或 <style> → 下载并解析 CSS       │
│       ↓                                         │
│  4. 将 CSS 规则应用到 DOM 树上 → CSSOM           │
│       ↓                                         │
│  5. 遇到 <script> → 下载并执行 JavaScript         │
│       ↓                                         │
│  6. 合并 DOM + CSSOM → 渲染树（Render Tree）       │
│       ↓                                         │
│  7. 布局（Layout）—— 计算每个元素的位置和大小       │
│       ↓                                         │
│  8. 绘制（Paint）—— 把像素画到屏幕上               │
└─────────────────────────────────────────────────┘
```

**关键点：**
- HTML、CSS、JS 都是**纯文本文件**，你可以用任何文本编辑器打开和修改它们。
- 浏览器是**解析器 + 渲染引擎**，它把你写的文本转成像素。
- 没有"编译"步骤——修改代码 → 保存 → 刷新浏览器，改动立刻生效。这就是网页开发的**即时反馈循环**。

---

## 五、静态网页 vs 动态网页

| 类型 | 原理 | 例子 | 需要后端语言？ |
|------|------|------|:---:|
| **静态网页** | 服务器上存的是写好的 HTML 文件，谁访问都一样 | 个人主页、博客、文档站 | ❌ 不需要 |
| **动态网页** | 服务器根据请求**实时生成** HTML，不同用户看到不同内容 | 淘宝、微博、邮箱 | ✅ 需要 |

对于个人网页来说，**静态网页完全够用**。你不需要数据库，不需要后端服务，不需要服务器端编程语言。

---

## 六、制作个人网页的完整技术栈

### 最简方案（推荐从这开始）

```
              HTML + CSS
                 ↓
        一个 .html 文件就能跑
                 ↓
      托管到 GitHub Pages（免费）
                 ↓
      获得一个 https://xxx.github.io 的网址
```

只需要学两样东西：**HTML**（结构）和 **CSS**（样式）。

### 进阶方案（好看一点）

```
         HTML + CSS + JavaScript
                 ↓
      用静态网站生成器（如 Hugo、Jekyll）
      或直接用现代前端框架（React/Vue）
                 ↓
      托管到 GitHub Pages / Netlify / Vercel（都免费）
```

### 各方案对比

| 方案 | 需要学的 | 难度 | 适合场景 |
|------|---------|------|---------|
| 纯 HTML + CSS | HTML、CSS 基础 | ⭐ | 简单的个人主页、简历页 |
| HTML + CSS + JS | 以上 + JavaScript 基础 | ⭐⭐ | 带交互的个人主页 |
| Hugo / Jekyll / Astro | 以上 + 模板语法 + 命令行基础 | ⭐⭐⭐ | 个人博客、作品集 |
| React / Vue | HTML/CSS/JS + 框架思想 + Node.js | ⭐⭐⭐⭐ | 复杂交互的 Web 应用 |

---

## 七、手把手：从 0 到上线

### 第一步：创建你的第一个 HTML 文件

在你的电脑上新建一个文件夹 `my-website`，在里面创建一个 `index.html` 文件：

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>张三的个人主页</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }

    body {
      font-family: "Microsoft YaHei", "PingFang SC", sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .card {
      background: white;
      border-radius: 16px;
      padding: 48px;
      max-width: 480px;
      text-align: center;
      box-shadow: 0 20px 60px rgba(0,0,0,0.2);
    }

    .avatar {
      width: 120px;
      height: 120px;
      border-radius: 50%;
      object-fit: cover;
      margin-bottom: 24px;
    }

    h1 { font-size: 28px; color: #333; margin-bottom: 8px; }
    .tagline { color: #999; font-size: 14px; margin-bottom: 24px; }
    .bio { color: #666; line-height: 1.8; margin-bottom: 32px; }

    .links a {
      display: inline-block;
      margin: 0 8px;
      padding: 10px 24px;
      background: #667eea;
      color: white;
      text-decoration: none;
      border-radius: 8px;
      font-size: 14px;
      transition: background 0.3s;
    }

    .links a:hover { background: #764ba2; }
  </style>
</head>
<body>
  <div class="card">
    <img class="avatar" src="https://via.placeholder.com/120" alt="头像">
    <h1>张三</h1>
    <p class="tagline">软件工程师 · 终身学习者</p>
    <p class="bio">
      热爱技术与创造。这里是我的数字小角落，<br>
      记录学习、思考和生活的点滴。
    </p>
    <div class="links">
      <a href="https://github.com">GitHub</a>
      <a href="mailto:hello@example.com">邮件</a>
      <a href="/blog">博客</a>
    </div>
  </div>
</body>
</html>
```

保存后，**直接用浏览器打开这个文件**（双击或拖到浏览器窗口），你就能看到一个完整的个人主页了。

### 第二步：理解你刚刚做了什么

- `<style>` 标签里的 CSS 直接写在了 HTML 文件里（这叫**内部样式表**）。
- CSS 做了：紫色渐变背景、白色圆角卡片、居中布局、头像圆形裁剪、按钮的悬停效果。
- 没有一行 JavaScript——这个页面是**纯静态**的，却已经很好看。

### 第三步：部署到互联网

做完的页面只有你电脑能看到，想让别人也能访问，需要把它放到一个**公网服务器**上。对个人网页来说，最简单（且免费）的方式是：

| 平台 | 特点 |
|------|------|
| **GitHub Pages** | 免费，绑定 git 仓库，自动部署 |
| **Netlify** | 免费，拖拽文件夹即可上线，支持自定义域名 |
| **Vercel** | 免费，同上，全球 CDN 加速 |

以 GitHub Pages 为例：
1. 注册 GitHub 账号
2. 创建一个名为 `你的用户名.github.io` 的仓库
3. 把 `index.html` 上传到这个仓库
4. 等几秒钟，访问 `https://你的用户名.github.io`，全世界都能看到了

---

## 八、常见疑问解答

### Q: 需要安装什么软件？

**最少：** 浏览器（你已经在用了）+ 文本编辑器（推荐 [VS Code](https://code.visualstudio.com/)，免费，有语法高亮和自动补全）。

**不需要：** Node.js、Python、PHP、MySQL、Docker……这些对纯静态个人网页都不是必需的。

### Q: HTML、CSS、JS 应该先学哪个？

**先 HTML**（理解页面结构，1-2 天）→ **再 CSS**（让页面好看，3-7 天）→ **最后 JavaScript**（让页面有交互，按需学）。

### Q: 静态网页是不是太"低级"了？

不是。现代很多公司官网、技术文档、个人博客都是纯静态的。静态 ≠ 简陋，动态 ≠ 高级。静态网页的优势：**极快、极安全、零维护成本、免费托管**。

### Q: 没有服务器，怎么处理表单提交？

可以使用第三方服务：
- **Formspree** / **Netlify Forms**：处理联系表单，免费额度够用
- **Disqus** / **Giscus**：给静态网页加评论功能

### Q: Node.js 是什么？做网页需要它吗？

**Node.js 是一个让 JavaScript 脱离浏览器运行的环境。** 这是理解它的关键。

回忆一下：JavaScript 本来是只能跑在浏览器里的语言。2009 年，有人把 Chrome 的 JS 引擎（V8）单独拿出来，加上文件读写、网络请求等能力，做成了 Node.js。从此 JavaScript 也能像 Python 一样在命令行里运行了。

```
JavaScript 的两种运行环境：

  浏览器里                           Node.js 里
  ┌─────────────────┐              ┌─────────────────┐
  │ 操作网页 DOM     │              │ 读写文件          │
  │ 响应用户点击     │              │ 启动 HTTP 服务    │
  │ 发起网络请求     │              │ 操作数据库        │
  │ 不能读写本地文件  │              │ 不能操作网页 DOM  │
  └─────────────────┘              └─────────────────┘
```

**做纯静态网页不需要 Node.js。** 但如果你的页面要越来越复杂，Node.js 会带来三个便利：

| Node.js 提供的 | 干什么用 | 什么时候需要 |
|:--|:--|:--|
| **npm**（包管理器） | 下载和管理第三方库（React、Tailwind CSS、图标库……） | 不想每个效果都从零手写 |
| **开发服务器** | 本地启动一个服务器，保存文件后浏览器自动刷新 | 多页面项目、不想手动刷新浏览器 |
| **构建工具** | 把现代 JS/CSS 代码编译成所有浏览器都支持的版本 | 用了 TypeScript、React/Vue 等框架 |

**一句话：** 如果只是做一个简单的单页个人主页，不需要 Node.js。如果要做博客或更复杂的项目、想享受"保存即刷新"的便利，Node.js 会很有帮助。

### Q: 用 Claude Code 辅助做网页，需要 Node.js 吗？

**不需要。Claude Code 不依赖 Node.js 运行，它帮你写代码也不需要 Node.js。**

Claude Code 能做的事：
- ✅ 用 `Write` / `Edit` 工具直接在项目里创建和修改 HTML、CSS、JS 文件
- ✅ 用 `Bash` 工具执行命令（前提是你系统里有对应的程序）
- ✅ 在你没有任何运行时的情况下，写出完整的静态网页

什么情况下会需要 Node.js：
- 当你让 Claude Code 运行 `npm install` 或 `npm run dev` 来启动开发服务器
- 当你让 Claude Code 用 Vite、Astro 等需要 Node.js 的框架搭建项目
- 当你需要跑单元测试（Jest、Vitest 等需要 Node.js）

**但这些都是"可选升级"，不是"入门要求"。** Claude Code 完全可以在一个只有浏览器和文本编辑器的环境里，帮你从零写出一个好看的个人网页。

---

## 九、学习路线建议

```
第 1 天：HTML 基础
  → 标签语法、常用标签（h1-h6, p, a, img, div, span, ul, li）
  → 资源：MDN Web Docs (developer.mozilla.org)

第 2-3 天：CSS 基础
  → 选择器、盒模型、颜色、字体、背景
  → Flexbox 布局（现代网页布局的核心）

第 4-5 天：动手做
  → 先模仿一个你喜欢的网页
  → 再改成你自己的内容

第 6-7 天：部署上线
  → 学一点 Git 基础
  → 部署到 GitHub Pages
  → 买一个域名绑上去（可选，约 50 元/年）

持续：JavaScript 入门
  → 变量、函数、事件、DOM 操作
  → 按需深入学习
```

---

## 十、总结

```
个人网页的最小知识集：

  ┌──────────┐
  │   HTML   │ ← 结构（必须）
  ├──────────┤
  │   CSS    │ ← 样式（必须，不然很丑）
  ├──────────┤
  │JavaScript│ ← 交互（可选，没有也能用）
  ├──────────┤
  │  浏览器   │ ← 唯一的"解释器"，你已经有
  ├──────────┤
  │ 文本编辑器 │ ← 唯一的工具，记事本都行
  └──────────┘

不需要安装任何解释器、运行时或数据库。
网页就是文本文件，浏览器就是它们的解释器。
```

从今天开始，你只需要一个文本编辑器和浏览器，就能做出自己的个人网页。
