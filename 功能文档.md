# 早哦早餐铺 - 网站项目说明

## 项目定位

这是一个部署在 GitHub Pages 上的个人记录网站，用于整理项目、原创音乐、照片、随笔，以及动画、漫画和轻小说喜好。网站以粉色毛玻璃和壁纸为主要视觉语言，访客以浏览公开内容为主，只有站主可以维护内容。

线上地址：<https://translucent-i.github.io/>

## 技术结构

| 层级 | 当前实现 | 作用 |
| --- | --- | --- |
| 页面 | `index.html` | 单页应用，包含 HTML、CSS 和 JavaScript |
| 静态资源 | `img/`、`favicon/` | 头像、图标、播放器图片等 |
| 静态托管 | GitHub Pages | 免费发布网页和静态资源 |
| 数据与认证 | Supabase | PostgreSQL、Auth、Storage |
| 音频播放 | HTML Audio + Web Audio API | 播放歌曲、音量、进度、律动条 |
| 作品搜索 | Bangumi、AniList 公共 API | 为动画、漫画、轻小说记录提供搜索结果 |

网站没有自建服务器。浏览器直接读取 GitHub Pages 页面，并通过 Supabase 的公开读取权限获取展示数据。

## 页面与功能

### 主页

- 左侧为站主概览：头像、签名、项目/照片/随笔数量和外部链接。
- 右侧为迷你音乐播放器，可选择歌单、播放、切歌、调节音量和展开歌曲浮层。
- 中部是音乐律动条、时段问候和时钟。
- 下方速览展示最近随笔、最近喜好和项目记录，可跳转至对应页面。

### 项目

- 卡片展示项目名称、简介和标签。
- 支持搜索、详情页与 Markdown 内容渲染。
- 站主可新建、编辑、删除项目，或导入 Markdown 文件。

### 音乐

- 支持多个原创歌单、歌曲上传、切歌、随机/循环、进度和音量控制。
- 歌单列表在固定区域内滚动，不会拉长页面。
- 歌曲文件存入 Supabase Storage 的 `songs` bucket，歌曲元数据存入 `songs` 表。
- 站主可新建/删除歌单、上传或删除歌曲，并补充歌曲简介和歌词。

### 喜好

- 分为喜欢的动画、喜欢的漫画、喜欢的轻小说三栏。
- 每栏支持站主自定义分类，卡片区可独立滚动。
- 每个记录可保存封面、名称、原名、年份、状态、进度、评分和短评。
- 点击卡片打开详情弹窗。
- 新增记录时可用 Bangumi/AniList 搜索，也可以手动填写。

### 照片墙与壁纸

- 照片墙展示公开照片，点击可放大查看。
- 站主可上传、删除照片和壁纸。
- 上传图片会压缩为 JPEG，并清除原图片 EXIF 元数据，减少定位信息泄露。
- 照片和壁纸分别使用 Supabase Storage 的 `photos`、`wallpapers` bucket。

### 随笔与关于

- 随笔按时间倒序展示，当前仅加载最近 30 条以控制数据库请求。
- 站主可发布、编辑、删除随笔。
- 关于页内容保存在浏览器本地偏好中，可由站主编辑。

## 访问与权限

### 访客能看到什么

访客可以浏览页面文字、项目、公开照片、壁纸、歌曲、随笔和喜好记录。公开 Storage 中的媒体文件可被网页播放或下载，因此不要上传私密照片、联系方式、精确位置信息或未准备公开的音乐。

### 站主如何进入管理模式

普通网址不会显示登录按钮：

```text
https://translucent-i.github.io/
```

站主使用以下地址打开登录入口：

```text
https://translucent-i.github.io/?admin=1
```

使用 Supabase Auth 中的站主邮箱和密码登录后，网页会根据 Supabase RLS 权限显示上传、编辑、删除、新建分类和新增记录等控件。

`?admin=1` 只负责隐藏或显示登录界面，不是安全机制。真正的写入限制由 Supabase Auth、RLS Policy 和 Storage Policy 执行。

## Supabase 数据

主要使用的数据表：

| 表 | 内容 |
| --- | --- |
| `projects` | 项目资料与 Markdown 内容 |
| `playlists`、`songs` | 歌单与歌曲元数据 |
| `notes` | 随笔 |
| `comments`、`likes` | 历史社交数据，目前公开访客只读 |
| `favorites`、`favorite_categories` | 动画、漫画、轻小说记录与分类 |
| `profiles` | 站主昵称、头像等资料 |

常用维护 SQL：

| 文件 | 用途 |
| --- | --- |
| `supabase-security.sql` | 公开读取、仅站主写入的 RLS 与查询索引 |
| `supabase-storage-policies.sql` | `songs`、`photos`、`wallpapers` 的公开读取、站主写入规则 |
| `supabase-add-light-novel.sql` | 允许动画、漫画、轻小说三种喜好类型 |
| `supabase-favorite-category-scope.sql` | 允许不同喜好类型使用同名分类（同一类型内仍不可重复） |

第一次使用轻小说栏前，需要在 Supabase SQL Editor 执行 `supabase-add-light-novel.sql`。
若动画、漫画或轻小说之间无法添加同名分类，需要再执行 `supabase-favorite-category-scope.sql`。

完整安全检查步骤见 [安全部署检查清单.md](安全部署检查清单.md)。

## 日常维护流程

1. 使用 `?admin=1` 地址登录站主账号。
2. 在对应页面完成上传、编辑或删除。
3. 使用无痕窗口打开普通网址，确认访客只看到公开展示内容。
4. 修改网页代码后，在项目目录执行：

```powershell
git add index.html
git commit -m "describe the change"
git push origin main
```

GitHub Pages 会在推送 `main` 后自动更新，通常需要 1-2 分钟。

## 本地预览

直接双击 `index.html` 可查看大部分静态界面。涉及 Supabase 请求时，建议在项目目录启动本地 HTTP 服务：

```powershell
python -m http.server 4173 --bind 127.0.0.1
```

随后访问 <http://127.0.0.1:4173/>。

## 重要边界

- 前端出现的 Supabase `anon` key 属于公开客户端配置；绝不能把 `service_role` key 写入网页或 GitHub 仓库。
- GitHub Pages 负责静态文件发布，普通个人访问量不会出现传统服务器内存不足问题。
- 需要关注的配额是 Supabase 数据库、Storage 容量和媒体下载流量。
- 目前歌曲、照片、壁纸为公开媒体。若未来需要存放不公开的原创文件，需要改用私有 bucket 和受保护的服务端签名 URL，不能只依赖静态网页隐藏链接。
